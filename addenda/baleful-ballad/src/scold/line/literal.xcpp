// -*- c++ -*- in the SCOLD styling that is so fashionable these days
#divert <hpp>
#import scold.line.Item
namespace scold {
  namespace line {
    // syntactic sugary convenience
    inline auto literal(std::string const &) -> Item;
  }
}
#endiv
#divert <ipp>
auto scold::line::literal(std::string const &line) -> Item {
  // The line::Item constructor supplies its own noline() indicator
  return Item(line);
}
#endiv
