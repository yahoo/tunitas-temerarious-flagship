// -*- c++ -*- C++23 with S.C.O.L.D. stylings; For terms, see the LICENSE file at the top of the repository.
#divert <fpp>
#import want.regex.unbusted
namespace want {
  namespace regex {
    // see http://www.cplusplus.com/reference/regex/regex/
    //
    // Yes we did that.
    //   s/regex/pattern/g
    // so as to avoid the type alias name being the same as the namespace
    using pattern = unbusted::regex;
  }
}
#endiv
