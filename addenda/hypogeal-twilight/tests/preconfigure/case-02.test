#!/bin/sh
source ${0%/*}/library/rigging.sh || exit 255
new_m4=m5
explanation="with ${new_m4}, does preconfigure"
if testify -I ${new_m4?}; then
    echo "OK ($explanation)"
    exit 0
else
    echo "FAIL ($explanation)"
    exit 1
fi
