#!/bin/sh
source ${0%/*}/library/rigging.sh || exit 255
explanation="with an additional m4 library"
if testify -I m4 -I apple; then
    echo "OK ($explanation)"
    exit 0
else
    echo "FAIL ($explanation)"
    exit 1
fi
