#!/bin/sh
source ${0%/*}/../library/rigging.sh || exit 255
make_outdir
good ${boilerplate_exe?} \
    --mk-directory=${outdir?} \
    --with-vernacular-doggerel=../library/installation \
    --with-hypogeal-twilight=../library/installation \
    --with-nonstd-gcc=/opt/nonstd/gcc \
    --with-nonstd-cppunit=/opt/nonstd/cppunit \
    --with-std-scold=/opt/scold \
    --with-std-mysql=/usr \
    ${end}
