#!/bin/sh
__topdir=$(realpath ${0%/*}/../../..)
export PATH="${__topdir?}/libexec/temerarious-flagship:${__topdir?}/bin:$PATH"
exec < /dev/null
roff --help
roff --usage
roff -h
roff -b || test 0 != $?
roff --bogon || test 0 != $?
date | roff


